diff --git a/easy-ui-react/src/Button/Button.stories.tsx b/easy-ui-react/src/Button/Button.stories.tsx
index 808764fe..52eb39d8 100644
--- a/easy-ui-react/src/Button/Button.stories.tsx
+++ b/easy-ui-react/src/Button/Button.stories.tsx
@@ -15,6 +15,8 @@ import {
   getTextButtonsColorMapping,
 } from "../utilities/storybook";
 import { Button, ButtonProps } from "./Button";
+import { ThemeProvider } from "../Theme";
+import { VerticalStack } from "../VerticalStack";
 
 type Story = StoryObj<typeof Button>;
 
@@ -46,6 +48,36 @@ const meta: Meta<typeof Button> = {
 
 export default meta;
 
+export const OverrideWithTheme: Story = {
+  render: () => (
+    <>
+      <VerticalStack gap="2">
+        <div>
+          <ThemeProvider
+            theme={() => ({
+              "color.button.primary.filled.font.color":
+                "var(--ezui-color-neutral-900)",
+              "color.button.primary.filled.active.font.color":
+                "var(--ezui-color-neutral-900)",
+              "color.button.primary.resting.color":
+                "var(--ezui-color-yellow-500)",
+              "color.button.primary.hover.focus.color":
+                "var(--ezui-color-yellow-600)",
+              "color.button.primary.active.color":
+                "var(--ezui-color-yellow-700)",
+            })}
+          >
+            <Button color="primary">Test</Button>
+          </ThemeProvider>
+        </div>
+        <div>
+          <Button color="primary">Test</Button>
+        </div>
+      </VerticalStack>
+    </>
+  ),
+};
+
 export const Filled: Story = {
   render: Template.bind({}),
   args: {
diff --git a/easy-ui-react/src/Button/_mixins.scss b/easy-ui-react/src/Button/_mixins.scss
index 129ffae5..07a1cee2 100644
--- a/easy-ui-react/src/Button/_mixins.scss
+++ b/easy-ui-react/src/Button/_mixins.scss
@@ -135,11 +135,11 @@
 
 // prettier-ignore
 @mixin colorPrimary {
-  @include component-token("button", "filled.font.color", design-token("color.neutral.000"));
-  @include component-token("button", "filled.active.font.color", design-token("color.neutral.000"));
-  @include component-token("button", "resting.color", design-token("color.primary.500"));
-  @include component-token("button", "hover.focus.color", design-token("color.primary.600"));
-  @include component-token("button", "active.color", design-token("color.primary.700"));
+  @include component-token("button", "filled.font.color", design-token("color.button.primary.filled.font.color"));
+  @include component-token("button", "filled.active.font.color", design-token("color.button.primary.filled.active.font.color"));
+  @include component-token("button", "resting.color", design-token("color.button.primary.resting.color"));
+  @include component-token("button", "hover.focus.color", design-token("color.button.primary.hover.focus.color"));
+  @include component-token("button", "active.color", design-token("color.button.primary.active.color"));
 }
 
 // prettier-ignore
diff --git a/easy-ui-react/src/Theme/Theme.tsx b/easy-ui-react/src/Theme/Theme.tsx
index 929f077e..4c48b102 100644
--- a/easy-ui-react/src/Theme/Theme.tsx
+++ b/easy-ui-react/src/Theme/Theme.tsx
@@ -28,7 +28,7 @@ const invertedColorSchemes: Record<ColorScheme, ColorScheme> = {
 
 export type ThemeableColorScheme = Extract<ColorScheme, "light" | "dark">;
 export type ThemePreferences = { colorScheme: ThemeableColorScheme };
-export type ThemeCreator = (preferences: ThemePreferences) => Theme;
+export type ThemeCreator = (preferences: ThemePreferences) => Partial<Theme>;
 
 export type ThemeProviderProps = {
   /** Component tree to apply specified theme. */
@@ -212,7 +212,7 @@ function ColorSchemeContextProvider({
   );
 }
 
-function getThemeInstanceVariables(theme: Theme) {
+function getThemeInstanceVariables(theme: Partial<Theme>) {
   return Object.fromEntries(
     Object.entries(theme).map(([key, value]) => {
       const property = tokenSafeKebabCase(key);
@@ -221,7 +221,7 @@ function getThemeInstanceVariables(theme: Theme) {
   );
 }
 
-function renderThemeVariables(theme: Theme) {
+function renderThemeVariables(theme: Partial<Theme>) {
   const variables = getThemeInstanceVariables(theme);
   const css = Object.entries(variables)
     .map((entry) => entry.join(": ") + ";")
diff --git a/easy-ui-tokens/src/theme/light.json b/easy-ui-tokens/src/theme/light.json
index 0dac936b..6f949972 100644
--- a/easy-ui-tokens/src/theme/light.json
+++ b/easy-ui-tokens/src/theme/light.json
@@ -78,6 +78,17 @@
     "color.neutral.050": { "value": "{color.gray.050}" },
     "color.neutral.025": { "value": "{color.gray.025}" },
     "color.neutral.000": { "value": "{color.gray.000}" },
+    "color.button.primary.filled.font.color": {
+      "value": "{color.gray.000}"
+    },
+    "color.button.primary.filled.active.font.color": {
+      "value": "{color.gray.000}"
+    },
+    "color.button.primary.resting.color": { "value": "{color.blue.500}" },
+    "color.button.primary.hover.focus.color": {
+      "value": "{color.blue.600}"
+    },
+    "color.button.primary.active.color": { "value": "{color.blue.700}" },
     "font.family": { "value": "{font.family.sans}" }
   }
 }
