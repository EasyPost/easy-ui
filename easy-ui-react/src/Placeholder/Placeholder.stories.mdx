import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { Placeholder } from "./Placeholder";
import { createLabelledOptionsControl } from "../utilities/storybook";

<Meta
  title="Placeholder"
  argTypes={{
    message: { control: "text" },
    iconSize: createLabelledOptionsControl({
      md: "md",
      lg: "lg",
      "Responsive -- xs-sm md-md lg-lg": { xs: "sm", md: "md", lg: "lg" },
    }),
  }}
  component={Placeholder}
/>

export const Template = (args) => <Placeholder {...args} />;

# Placeholder

With `MDX`, we can define a story for `Placeholder` right in the middle of our
Markdown documentation.

<Canvas>
  <Story
    name="Default"
    args={{
      status: "info",
      message:
        "This is a placeholder component to test the initial plumbing of Easy UI. It will be removed once the plumbing is complete.",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
