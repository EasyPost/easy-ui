import React from "react";
import { ArgTypes, Canvas, Meta, Controls } from "@storybook/addon-docs/blocks";
import { ColorPickerInputField } from "./ColorPickerInputField";
import * as ColorPickerInputFieldStories from "./ColorPickerInputField.stories";

<Meta of={ColorPickerInputFieldStories} />

# ColorPickerInputField

A `<ColorPickerInputField />` allows users to select a color with a color picker or as a text input.

<Canvas of={ColorPickerInputFieldStories.Default} />

<Controls of={ColorPickerInputFieldStories.Default} />

## Properties

<ArgTypes of={ColorPickerInputField} exclude={["children"]} />
