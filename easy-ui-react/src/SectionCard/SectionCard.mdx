import React from "react";
import { ArgTypes, Canvas, Meta, Controls } from "@storybook/blocks";
import { SectionCard } from "./SectionCard";
import * as SectionCardStories from "./SectionCard.stories";

<Meta of={SectionCardStories} />

# SectionCard

Card serving as a section in the product.

## WithTabs

SectionCard with tabs.

<Canvas of={SectionCardStories.WithTabs} />

<Controls of={SectionCardStories.WithTabs} />

## Properties

### SectionCard

<ArgTypes of={SectionCard} />
