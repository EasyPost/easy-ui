import React from "react";
import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { Button } from "../Button";
import { Tooltip } from "./Tooltip";

<Meta title="Components/Tooltip" component={Tooltip} />

# Tooltip

<Canvas>
  <Story name="Default">
    <div style={{ display: "flex", height: "100vh" }}>
      <div className="here" style={{ display: "inline-flex", margin: "auto" }}>
        <Tooltip
          text="Deleting will remove this record permanently"
          placement="top start"
          defaultOpen={true}
          offset={12}
          crossOffset={12}
        >
          <span style={{ display: "inline-flex" }}>
            <Button>Delete 2</Button>
          </span>
        </Tooltip>
      </div>
    </div>
  </Story>
</Canvas>
