import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import ArrowBackIcon from "@easypost/easy-ui-icons/ArrowBack";
import AddIcon from "@easypost/easy-ui-icons/Add";
import {
  ButtonStoryDecorator,
  ButtonStoryOnDarkBackgroundDecorator,
} from "../Button/Button.stories.mdx";
import { createLabelledOptionsControl } from "../utilities/storybook";
import { Icon } from "../Icon";
import { IconButton } from "./IconButton";

<Meta title="Components/Button/IconButton" component={IconButton} />

export const sharedIconButtonProps = {
  symbol: ArrowBackIcon,
  accessibilityLabel: "arrow back",
};

export const Template = (args) => <IconButton {...args} />;

# IconButton

<Canvas>
  <Story
    name="Default"
    args={{
      icon: <Icon {...sharedIconButtonProps} />,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Filled buttons

<Canvas>
  <Story name="FilledButtons" decorators={[ButtonStoryDecorator]}>
    <IconButton icon={<Icon {...sharedIconButtonProps} />} />
    <IconButton color="secondary" icon={<Icon {...sharedIconButtonProps} />} />
    <IconButton color="success" icon={<Icon {...sharedIconButtonProps} />} />
    <IconButton color="warning" icon={<Icon {...sharedIconButtonProps} />} />
    <IconButton color="neutral" icon={<Icon {...sharedIconButtonProps} />} />
  </Story>
</Canvas>

## Outlined buttons

<Canvas>
  <Story name="OutlinedButtons" decorators={[ButtonStoryDecorator]}>
    <IconButton variant="outlined" icon={<Icon {...sharedIconButtonProps} />} />
    <IconButton
      color="secondary"
      variant="outlined"
      icon={<Icon {...sharedIconButtonProps} />}
    />
    <IconButton
      color="success"
      variant="outlined"
      icon={<Icon {...sharedIconButtonProps} />}
    />
    <IconButton
      color="warning"
      variant="outlined"
      icon={<Icon {...sharedIconButtonProps} />}
    />
    <IconButton
      color="neutral"
      variant="outlined"
      icon={<Icon {...sharedIconButtonProps} />}
    />
  </Story>
</Canvas>

## Inverse buttons

<Canvas>
  <Story
    name="InverseButton"
    decorators={[ButtonStoryOnDarkBackgroundDecorator]}
  >
    <IconButton
      color="inverse"
      variant="outlined"
      icon={<Icon {...sharedIconButtonProps} />}
    />
  </Story>
</Canvas>

## Disabled buttons

<Canvas>
  <Story name="DisabledButtons" decorators={[ButtonStoryDecorator]}>
    <IconButton icon={<Icon {...sharedIconButtonProps} />} isDisabled />
    <IconButton
      variant="outlined"
      icon={<Icon {...sharedIconButtonProps} />}
      isDisabled
    />
  </Story>
</Canvas>

## Click event

<Canvas>
  <Story name="ClickEvent" decorators={[ButtonStoryDecorator]}>
    <IconButton
      icon={<Icon {...sharedIconButtonProps} />}
      onPress={() => alert("clicked!")}
    />
  </Story>
</Canvas>

<ArgsTable story="Default" />
